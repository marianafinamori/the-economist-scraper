
            <header class="header-school-page navbar justify-content-center sticky-top">
                <h1 class="logo-scholar-line">
                        MONGO SCRAPER
                </h1>
            </header>
            <div class="nav-container">
                <!-- <nav class="nav nav-school justify-content-center"> -->
                <ul class="nav justify-content-center">
                    <li class="link-container nav-item">
                        <a class="home-link nav-link" href="/">HOME</a>
                    </li>
                    <li class="link-container nav-item">
                        <a class="saved-link nav-link" href="/saved">SAVED ARTICLES</a>
                    </li>
                    <li class="link-container nav-item">
                        <form class="form-inline"> 
                            <button id="scrape-btn" class="btn btn-dark my-2 my-sm-0" type="submit">SCRAPE ARTICLES</button>
                        </form>
                    </li>
                </ul>
            
        </div>
        {{!-- NAVBAR END --}}

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h2>Saved articles</h2>
            {{#each articles}}
    <div class="card mt-5">
        <div class="card-body bg-success">
            <h5 class="card-title article-title">{{this.title}}</h5>
            <p class="card-text article-summary">{{this.descr}}</p>
    
            <br>
            <a href='{{this.link}}' target="_blank"><button class="btn btn-primary btn-dark mt-4">KEEP READING</button></a>
            <button class="btn btn-primary btn-dark ml-3 mt-4 remove-saved-btn" data-id="{{this._id}}">DELETE FROM FAVORITES</button></a>
            <button class="btn btn-dark ml-3 mt-4 add-notes-btn" data-id="{{this._id}}">COMMENT</button>
        </div>
    </div>
{{/each}}
        </div>
    </div>
</div>



